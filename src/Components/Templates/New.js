import React from 'react';

const PrintDocument = () => {
    return (
        <form>
            <h1>Sorry, this feature is not available yet.</h1>
        </form>
    );
}

export default PrintDocument;